# LittleWhiteBox 总结增强版 · Memory+ 5

更新日期：2026-09-16

版本标识：`3.1.3-memory.5`

上游基础：`RT15548/LittleWhiteBox`，提交 `4db9202080cab1c3116de9a027d8c8bb5e048787`。这是独立修改版，不是上游官方发布。

## 这次改了什么

### Memory+ 5：人物关系自动新增与显示修复

- 统一识别“对某人的看法”“与某人的关系”及对应繁体写法，修复清洗能接收、面板却无法识别的情况；原文姓名不做简繁转换。
- 兼容 `relationshipUpdates`、`relationships`、`characters.relationships` 中的 `{from,to,label,trend}` 输出，合并到同一份 `factUpdates`；新旧格式同时出现时以正式 `factUpdates` 为准。
- 关系更新默认作为核心事实；即使旧关系漏标了核心，也不参与普通事实容量清理。缺失关系增量不会删除手工关系；关系有新依据时仍可正常更新，不等同于锁定人物性格。
- 每批单列【已记录人物关系】，检查首次关系、遗漏关系与关系变化。要求有依据的初次关系也要提取，不必等待“关系再次变化”；不能只写人物名单、事件、基础档案或弧光。
- 已有事实里存着、但因上述写法未显示的关系，升级并刷新后即可识别。尚未提取的关系在后续总结中依据本批新对话及【已记录事件】尝试补建；不会重新扫描全部历史，不保证模型一次补齐，无依据不能补。
- 调试日志新增 `关系增量: 本批 N 条（含删除），合并后共 M 条`。这里只统计清洗后可识别的输出，不代表模型已发现所有应有关系。

### 0a. Memory+ 4：修好自动总结漏掉「人物基础档案 / 世界观设定」

之前自动总结时，模型经常完全不输出人物档案与世界观设定的增量，面板里的档案因此一直是空的。原因是两段相关指令被放在整段提示词的最末尾，而且只写成「可选」，模型很容易直接忽略。

这次做的改动：

- 把“人物基础档案增量”“世界观设定增量”两段指令从提示词末尾提到规格区（输出格式之前），和事实、别名等规则排在一起。
- 指令改为“每批必查”，并列出三条明确检查项：本批是否有新的稳定信息、已有空白字段能否用本批对话或【已记录事件】补全、是否有需要修正的旧值。确实没有内容时才允许省略这个 key。
- 发给模型的“已有总结”里补上了【已有世界观设定】整块内容和两份【待补全字段】清单，模型能直接看到哪些字段还是空的，不用自己猜。
- 兼容模型给的各种松散写法：字段值可以直接给字符串，可以扁平放在条目上，证据可以写在字段里也可以写在条目上；缺字段不会清空旧值，没证据的内容只进“待审核”。
- 调试日志新增一行 `档案增量: profileUpdates N 条 → X 条变更；loreUpdates N 条 → Y 条变更`；模型给了更新却没写进去时会额外提醒，方便定位是不是字段名不认识。

**注意：这次修复不会自动找回之前已经丢掉的设定。** 建议先手动把当前剧情里还在用的人物档案和世界观设定补齐（新建人物 / 补齐空白档案 / 世界观设定的手动补录都可），之后的总结才会持续维护它们。

### 0. Memory+ 3：世界观设定（固定设定）

右侧新增“世界观设定”面板，用来记录剧情里不该每次被刷掉的世界细节，例如特殊城市与地点、特殊物品与道具、药水与药剂、魔法与能力体系、势力与组织，也可以选“其他设定”或手动补录。

- 每条设定包含：名称、别名、类别、四个字段（定义与概述 / 具体设定 / 规则与限制 / 其他补充）。
- 每个字段默认锁定。已有锁定值不会被后续 AI 总结直接替换；冲突内容进入“待审核”，可以接受并锁定或拒绝建议。
- 每项 AI 更新都必须带 `value` 和 `evidence`；没有证据的更新不入库，缺字段不会清空旧值。
- 每条设有“常驻注入”开关：勾选后随总结提示词一起注入，不依赖向量召回命中，并在“注入位置”里占有独立字符预算（默认 4,000 字符，可调 1,000–32,000）。超出预算时按完整字段省略并给出提示。
- 面板上有“预览世界观设定（不调用模型）”，可核对最终会注入哪些设定、是否被预算省略。
- 注入顺序为：人物基础档案 → 世界观设定 → 本轮召回的剧情记忆。设定按 `【类别】名称` 分行给出，模型可据此区分城市、物品、药水与魔法规则。
- 总结提示词新增可选的 `loreUpdates` 字段；只有原文明确支持、后续需要保持一致的世界观内容才会被提取。一次性道具、临时场景、情绪与关系变化仍走事件 / 事实 / 弧光。

**和人物基础档案一样，第一次升级不会凭空恢复之前已经丢掉的设定。** 可以先手动补录当前剧情仍在使用的城市、物品和规则，之后的总结才会开始自动提取。

### 1. 事件可以插入中间

进入“剧情时间线 → 编辑”，每条事件提供“在前插入 / 在后插入 / 上移 / 下移”。顶部也可以在开头插入、末尾追加、搜索和撤销结构操作；支持 `Alt + ↑ / ↓`。

- 移动直接操作原有编辑卡片，不会丢失尚未保存的输入。
- 保存后，时间线和提示词呈现遵循人工顺序；向量模式仍按相关性选事件，但同组命中事件按人工顺序呈现。
- 排序写入独立 `sortOrder`，不伪造事件原文楼层，不改变现有 ID 和有效因果关联。
- 新增事件记录为当前聊天楼层的人工补录；旧事件保留来源楼层。
- 取消不写入聊天；撤销按钮撤销本次编辑中的插入、删除和移动，不是文本输入的撤销。

### 2. 固定人设与关系成长分开

新面板叫“人物基础档案”；原来只有弧光和关系的“人物档案”改为“关系与成长”，原功能保留。

基础档案包含：身份背景、外貌、稳定性格、价值观与底线、说话方式、长期动机、能力限制、其他固定设定。

- 每个字段默认锁定。已有锁定值不会被后续 AI 总结直接替换；冲突更新进入“待审核”。
- 空白字段可以从后续总结的明确证据补充；没有返回某字段，不会清空旧值。
- 每项 AI 更新必须有 `value` 和 `evidence`，不能把临时情绪、好感或位置当作固定性格。
- 可接受并锁定、拒绝建议；相同待审建议不重复入队，已拒绝的相同建议在拒绝记录保留期间不再重复提出。
- 手动修改和自动补充有记录；每个人物最多保留最近 200 条修改记录、100 条待审建议。人工解除锁定后，该字段允许自动更新。
- 支持别名、搜索、从主要人物创建空白档案、删除后恢复。所有编辑和审核操作都需要最后点击“保存”。
- 勾选“常驻注入”的底稿独立加入总结提示词，不依赖向量召回命中；在非向量模式，以及还未进行首次总结但已有聊天消息时也可注入。
- 常驻档案有独立字符预算，默认 8,000，可调至 1,000–32,000。超出时按完整字段省略，并提供提示；不是按 token 计算。人物很多时应缩写底稿、调整预算或取消次要人物常驻。

**第一次升级不会凭空恢复以前已经丢失的设定。** 可以先点击“补齐主要人物空白档案”，把角色卡或仍保留的早期聊天中的确定设定填入。之后的总结才开始自动提取基础信息；本版没有偷偷重跑全部历史、也不会自动调用模型补旧档案。

关系与弧光仍是动态状态，不会整体冻结，也不会自动推翻基础档案。本版降低因基础设定被覆盖造成的漂移，但不能保证任何模型绝不 OOC，也不能消除角色卡、世界书或其他提示词之间的冲突。

### 3. 面板中调整注入位置

顶部“注入位置”直接打开设置中的对应区域：

- **自动**：跟随总结 / 向量边界，保留原来的自动深度逻辑，限制在实际聊天消息范围内。
- **手动固定深度**：从聊天末尾向前数消息；`0` 是末尾，`3` 是距末尾三条消息。超过聊天长度时使用实际长度。
- **强制插入到聊天最后**：优先使用深度 `0`，固定深度控件暂时禁用；关闭后恢复原策略。
- System / User / Assistant 角色仍在同一区域选择。这些位置设置是全局默认，不是单聊天独立配置。
- “预览人物底稿”只展示已保存的常驻档案和预算省略情况，不调用模型，也不假装展示本轮真实向量召回结果。
- 位置设置不会改变隐藏楼层；“当前聊天启用”关闭后仍停止全部总结记忆注入。

设置保存后底部显示“配置已保存”。编辑底稿或注入配置时会取消旧预取结果，避免下次继续使用旧底稿或旧深度。

## 安装前先备份

1. 在原总结面板复制记忆包，保存为 JSON 文本。
2. 使用酒馆的聊天导出 / 备份功能，保留完整聊天和相应元数据备份；有向量数据时也单独导出。记忆包本身不包含完整聊天或全部向量。
3. 备份服务器上现有 LittleWhiteBox 插件目录。不要仅依靠浏览器缓存。
4. 最好先复制一份聊天作验证，不要直接在唯一的长期聊天上试验回滚、清空或导入覆盖。

## 安装到你实际运行酒馆的机器

### 方式 A：从 Git 仓库安装（远程托管面板常用）

安装地址：`https://github.com/zzhou0807/LittleWhiteBox-MemoryPlus`

1. 已经从这个地址安装过：先备份，再在扩展管理中更新这一份插件，不需要卸载或另装第二份。
2. 尚未安装：在“安装扩展”的 Git URL 输入框填入上述地址。仓库公开，不需要填写 GitHub 令牌。
3. 更新后使用 `Ctrl + Shift + R` 强制刷新，打开总结面板确认显示 **Memory+ 5**（`3.1.3-memory.5`）。
4. 不要同时启用原版和改版。推送新代码不等于你的远程酒馆已经更新，需要由扩展管理或服务器部署拉取。

### 方式 B：直接替换服务器上的插件目录（最稳）

交付 ZIP 内的 `LittleWhiteBox/` 是完整扩展目录，包含修改后的源码，不含 `.git`、`node_modules`、测试输出或个人聊天数据。

1. 暂停生成，关闭总结面板。
2. 解压 ZIP，找到其中 `LittleWhiteBox/manifest.json`。
3. 在**运行 SillyTavern 服务的机器 / 容器**上找到已经安装的 LittleWhiteBox 目录，用解压后的文件替换该目录内容。保持原来的扩展目录名；不要把整个压缩包再嵌套成 `LittleWhiteBox/LittleWhiteBox/`。
4. 不要同时启用原版和增强版两份插件，避免重复注册生成钩子或双重注入。
5. 按你的部署方式重启酒馆服务，然后关闭旧页面，重新打开并强制刷新浏览器缓存（Windows Chrome 可用 `Ctrl + Shift + R`）。
6. 打开总结面板，确认副标题出现 **Memory+ 5**，并看到“人物基础档案”“世界观设定”“注入位置”。扩展清单版本为 `3.1.3-memory.5`。

**只把 ZIP 下载到自己的电脑，不会改变远程服务器上的插件。** 没有服务器文件管理权限时，请使用方式 A 更新，或交给站点管理员部署。本次修复不直接改动线上酒馆或真实聊天。

不要混用仅单文件替换的方式；以完整目录部署和实际出现 Memory+ 5 标识为准。未对远程服务执行升级兼容实测。

## 升级后建议验收

1. 在复制的聊天里，在第二条事件前插入一条，保存并重开面板，核对顺序和原文楼层。
2. 新建一个人物底稿，填写性格、底线和目标，保持锁定并保存。
3. 在设置中预览底稿，确认这些字段存在；正式生成时还应查看酒馆实际发送的提示词，确认其他插件没有覆盖本插件的注入。
4. 以后出现待审更新，确认旧性格未改变，再决定是否接受。不要把一次生气等同于性格改变。
5. 切换固定深度，保存并重开设置确认数值；再检查酒馆最终提示词中的实际位置。
6. 新建一条世界观设定（例如某个特殊城市或药水），保持常驻与锁定并保存；再点“预览世界观设定”，确认它出现在注入文本里。模拟下一批总结时若出现冲突设定，确认旧值未变、建议进入“待审核”。
7. 复制增强版记忆包，在测试聊天中导入，检查底稿、世界观设定、锁定状态和审核记录。导入会覆盖当前总结，并按原插件流程清理向量 / 重设边界，务必只在已备份的目标上操作。

## 数据兼容与回退

- 旧记忆包没有 `profiles` / `lore` 时正常导入为空面板，不伪造人物基础资料或世界观设定。
- 新记忆包包含底稿、世界观设定、字段锁定、候选和记录；跨聊天导入会去掉旧来源楼层，避免把另一段聊天的楼层号误当成本地证据。
- 常规总结回滚把新档案与世界观设定的变化纳入同一逆操作；若后续人工改动与该批发生冲突，按原插件规则拒绝不安全回滚，不静默覆盖人工改动。
- 跨越没有逆操作的旧版历史时，保留基础档案与世界观设定，而不是假装可以精确重建旧底稿。
- “清空全部总结”仍会删除基础档案与世界观设定，面板已增加明确提醒；清空前先导出。
- 回退插件版本时，先保存增强版记忆包并恢复原插件目录。旧版不认识新增字段，其导入导出可能丢掉底稿与设定；不要把旧版导出的包当作增强版的完整备份。
- 原仓库一键更新可能覆盖本次修改。继续使用增强版期间，不要未经合并就直接用上游文件覆盖它。

## 验证范围

完成了总结模块离线测试、运行检查、回放构建和 Chrome 扩展本地界面验收（含世界观设定面板的增删改、审核、保存与注入预览）。详见同目录的 `总结增强版-验证记录.md`。

没有调用真实总结模型，没有修改远程酒馆设置或聊天，也没有声称已经在远程生产环境部署成功。本版采用原插件的静态 ES 模块加载方式，无须为总结面板单独运行 Vite 全站构建。

原作者与许可证信息保持不变，参见 `docs/LICENSE.md`。
