# LittleWhiteBox · Memory+ 3

> **这是个人改版，不是上游官方发布。**
>
> - 上游原项目：[RT15548/LittleWhiteBox](https://github.com/RT15548/LittleWhiteBox)
> - 改版基准提交：`4db9202080cab1c3116de9a027d8c8bb5e048787`
> - 本仓库是在原作者代码基础上做的本地修改（独立新建仓库，非上游官方仓库，也未在 GitHub 上建立 Fork 关系）。原作者署名与许可证保持不变，详见 `docs/LICENSE.md`。

一个面向 SillyTavern 的多功能扩展，包含剧情总结/记忆系统、变量系统、任务与多种面板能力。集成了画图、流式生成、模板编辑、调试面板等组件，适合用于复杂玩法与长期剧情记录。

## 本改版相对原版改了什么（Memory+ 1 / 2 / 3）

全部改动都是在原版基础上的追加与增强，没有删减原有能力：

1. **事件可在任意位置插入与排序**：剧情时间线支持「在前插入 / 在后插入 / 上移 / 下移 / 搜索 / 撤销结构操作」，排序写入独立 `sortOrder`，不改动事件原文楼层。
2. **人物基础档案（固定人设）**：新增独立面板，字段默认锁定，AI 更新必须携带证据，冲突进入「待审核」，避免每次总结把人物性格刷成新的导致 OOC；原「人物档案」改名「关系与成长」，原功能保留。
3. **可调注入位置**：面板顶部「注入位置」可在 自动 / 手动固定深度 / 强制置于聊天末尾 之间切换，并为人物质稿提供独立字符预算。
4. **世界观设定（固定设定）**：新增独立面板，用于记录剧情中出现的特殊城市、物品、道具、药水、药剂、魔法体系与势力组织，也可手动补录。每条设定有类别、别名、四个字段（定义与概述 / 具体设定 / 规则与限制 / 其他补充），字段默认锁定，AI 更新必须携带证据，冲突进入「待审核」；每条可单独勾选「常驻注入」，并有独立字符预算（默认 4,000）。

改版版本标识 `3.1.3-memory.3`（见 `manifest.json`，面板副标题显示 `Memory+ 3`）。安装、备份与回退说明见 `总结增强版-安装说明.md`，验证记录见 `总结增强版-验证记录.md`。

### 修复记录

- `3.1.2-memory.2`：修复在 HTTP 明文页面（非安全上下文，浏览器不提供 `crypto.randomUUID`）下「＋ 新建人物 / 补齐主要人物空白档案」点击无反应的问题；同时让这两个按钮在失败时显示原因、在无可用人物名时给出提示，不再静默无反应。
- `3.1.3-memory.3`：新增「世界观设定」面板（城市 / 物品 / 药水 / 魔法 / 势力等固定设定）、按类别注入、独立字符预算与预览；总结提示词新增可选的 `loreUpdates` 增量字段。

## 许可证

详见 `docs/LICENSE.md`。本改版沿用上游许可证，代码与设定版权归原作者所有。
