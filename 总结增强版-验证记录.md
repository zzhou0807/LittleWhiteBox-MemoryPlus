# Memory+ 5 验证记录

日期：2026-09-16

基线：`4db9202080cab1c3116de9a027d8c8bb5e048787`

## 自动测试

- 总结模块全量测试：229 项通过，0 失败，0 跳过。
- Memory+ 5 新增 13 项：简繁体与同义关系解析；旧式关系数组兼容；同向去重与清洗幂等；无证据不凭名单造边；普通事实、删除、非法趋势与零值兼容；关系目标别名归一；真实清洗 → 合并 → 面板投影 → 撤销；旧关系的 ID、来源、趋势保留；历史重复关系按最近状态合并且可撤销；25 批普通事实清理后关系不丢；真实上下文单列现有关系；实际模型消息装配包含关系检查且保留档案和世界观指令。全部使用虚构测试材料，未调用真实模型。
- Memory+ 1 新增 28 项：事件编辑与顺序、资料锁定、增量补全、候选审核、别名合并、回滚冲突、预算、公开人物投影、DOM 编辑器和宿主函数集成。
- Memory+ 3 新增 17 项：世界观设定的字段锁定与冲突拦截、证据校验、候选接受 / 拒绝与重复抑制、字段解锁后可更新、手动保存的记录与来源、按名称 / 别名去重、常驻预算按整字段省略、预算边界归一化、撤销往返、编辑器在无 `crypto` 的明文页面可用、类别与常驻开关保存、空名称 / 重名拦截、删除恢复、未保存输入保护、搜索只隐藏不删除、恶意文本转义，以及宿主真实合并与真实注入路径。
- Memory+ 4 新增 2 项，并调整 2 项：新增「档案与世界观增量指令内联进 JSON 格式提示且只出现一次」，以及「松散模型输出（纯字符串字段、扁平字段、条目级证据）可被正确接收」；原「无证据更新不会清空档案 / 设定」两项改为断言这类更新进入待审核而非直接写入。测试总量 214 → 216。
- 宿主集成测试从实际源码 AST 提取函数，在隔离依赖下执行，验证真实准备函数的向量 / 非向量 / 首次总结前注入路径，以及真实合并、导入导出路径；并非仅检查模拟实现。
- 向量运行检查：提示词一致性、会话释放、会话期间脏数据处理、运行时重启、缓存导入约束全部通过。
- 回放构建：通过。
- 修改过的 JavaScript：ESLint 错误级检查通过；保留上游既有的文档类警告，不为本功能全库重排格式。
- Git 补丁空白检查：通过。

## 既有 Chrome 扩展界面验收（Memory+ 3，2026-09-14）

以下为旧版验收记录；Memory+ 5 未重跑浏览器操作，以本次自动测试中的真实面板数据投影及运行时检查为准。

测试地址只监听本机，使用独立虚构故事，不操作远程酒馆：

- 实际总结 iframe 加载成功；未发现页面错误日志。
- 第二条事件前插入 → 填写 → 下移 → 撤销移动 → 保存 → 重开面板：顺序、正文保持正确。
- 修改长期动机、拒绝性格漂移建议、保存，再模拟下一批：旧性格不变，修改的目标不丢失，空白能力字段补全。
- 底稿预览包含稳定性格、底线、说话方式、长期动机与能力，并明确标注不是完整向量召回预览。
- 世界观设定：新建“雾灯（物品与道具）”、填写具体设定并保存后，面板立即出现该项；保存后状态栏提示“已保存到测试内存：lore”。
- 模拟下一批总结时，锁定的“规则与限制”未被覆盖，冲突内容进入“待审核 · 1”；编辑器中旧值保持、建议与依据 / 楼层显示完整；点“接受并锁定”后保留同卡片中未保存的“其他补充”输入，再保存才写入。
- “预览世界观设定（不调用模型）”按 `【类别】名称` 输出三条常驻设定，标注独立预算 4,000 字符、输出字符数与省略字段数。
- 末尾模式覆盖固定深度并禁用深度输入；关闭后恢复固定深度。
- 设置保存后底部显示“配置已保存”。
- 390 像素窄屏与暗色主题下检查编辑器：正文、输入和操作按钮可见，编辑框没有横向溢出。也检查了桌面亮色布局。

## 本地复现

先按仓库锁文件安装依赖，再执行：

```powershell
$tests = Get-ChildItem -LiteralPath modules/story-summary/tests -Filter '*.test.js' | ForEach-Object FullName
node --test @tests
node scripts/story-summary-runtime-check.mjs
node scripts/story-summary-replay-runner.mjs --build-only
node scripts/story-summary-preview/server.mjs
```

预览服务输出本地访问地址。页面上的“模拟下一批总结”只在内存中操作测试数据，不会请求模型。刷新顶层页面重置测试数据；“重开面板”保留本次测试内存。

本次测试运行于 Node 20.14.0。安装依赖时上游部分 Vite / Vue 工具链给出更高 Node 版本要求的警告，因此没有据此宣称全插件所有其他模块均已重新构建。总结功能本身是静态 ES 模块，其测试与回放构建已实际运行通过。

## 未覆盖

- 远程生产站点部署、服务器持久化权限与第三方插件组合。
- 真实模型生成质量和长期 OOC 发生率。
- 全部聊天历史的自动人物档案回填。
- 上游其他功能模块的完整构建与业务回归。
